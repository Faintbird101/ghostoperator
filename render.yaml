services:
  - type: web
    name: ghostoperator
    runtime: go
    rootDir: backend
    buildCommand: go build -o server ./cmd/server
    startCommand: ./server -static "" -port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false          # paste this in the Render dashboard
    healthCheckPath: /health
